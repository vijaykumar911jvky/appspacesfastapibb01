name: Trigger auto deployment for container-app-hasllly2bksri

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-hasllly2bksri-AutoDeployTrigger-53e6c713-8302-43e7-a2d6-59a02af1bde5.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPHASLLLY2BKSRI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPHASLLLY2BKSRI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPHASLLLY2BKSRI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPHASLLLY2BKSRI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPHASLLLY2BKSRI_REGISTRY_PASSWORD }}
          containerAppName: container-app-hasllly2bksri
          resourceGroup: appspacesfastapibb01b739
          imageToBuild: default/container-app-hasllly2bksri:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


